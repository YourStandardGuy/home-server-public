# Disable the go.d docker collector (Docker API polling every 3s = dockerd CPU spike).
# Per-container CPU/mem/IO still collected via cgroups plugin (reads files, no API).
# Overrides the stock default (docker: yes) in /usr/lib/netdata/conf.d/go.d.conf
docker: no
